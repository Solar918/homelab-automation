- name: Install Apache2
  ansible.builtin.apt:
    name: apache2
    state: present
    update_cache: yes

- name: Ensure Apache2 is running and enabled
  ansible.builtin.service:
    name: apache2
    state: started
    enabled: yes
